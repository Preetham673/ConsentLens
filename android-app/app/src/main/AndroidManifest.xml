<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.consentlens">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".ConsentLensApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.ConsentLens"
        android:usesCleartextTraffic="true">

        <!-- ✅ SPLASH — opens first -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ConsentLens.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- DASHBOARD — opened after splash -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".ComplianceDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".ConsentGraphActivity"
            android:exported="false" />

        <activity
            android:name=".MultiAppSimulationActivity"
            android:exported="false" />

        <activity
            android:name=".AppListActivity"
            android:exported="false" />

        <activity
            android:name=".RiskAlertActivity"
            android:exported="false" />

        <activity
            android:name=".ConsentHistoryActivity"
            android:exported="false" />

        <activity
            android:name=".AuditLogActivity"
            android:exported="false" />

        <activity
            android:name=".ComplianceReportActivity"
            android:exported="false" />

        <service
            android:name=".AppMonitorService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".AppInstallReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

    </application>

</manifest>